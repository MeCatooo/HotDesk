namespace HotDesk.Models
{
    public static class ResrvationLogic
    {
        //is there a reservation for desk in given time span
        //public static bool IsDeskReserved(Desk desk, DateTime startTime, DateTime endTime)
        //{
        //    foreach (Reservation reservation in desk.Reservations)
        //    {
        //        if (reservation.StartTime < endTime && reservation.EndTime > startTime)
        //        {
        //            return true;
        //        }
        //    }
        //    return false;
        //}

    }
}
